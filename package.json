{
  "name": "every-mile-counts",
  "version": "1.0.0",
  "description": "Every Mile Counts - endurance sports tracking and coaching platform",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "setup": "npm run setup --prefix server",
    "dev": "npx concurrently \"npm run dev:server\" \"npm run dev:client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
